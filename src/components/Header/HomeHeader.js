import React from "react";
import HeaderSlider from "../sliders";

const Header = () => {
  return (
    <header id="home" className="home-header">
      <HeaderSlider />
    </header>
  );
};

export default Header;
